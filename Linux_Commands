sudo su -
yum update -y
yum install -y httpd 
systemctl status httpd
mkdir temp
cd temp
-> wget
   wget(world wide get) is used to retrive or download the files or content from web servers via HTTP,HTTPS and FTP protocols with non-interactive downloads.
ls -lrt
unzip displayed_name.zip
ls -lrt
cd (main repo name)
ls -lrt
mv * /var/www/html/
cd /var/www/html
ls -lrt
systemctl enable httpd
systemctl start httpd
systemctl status httpd

-> history
This command is used to view the previously executed commands on the terminal
